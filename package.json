{
  "name": "franchisesim",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "cd server && npx prisma db push --accept-data-loss --skip-generate && (node prisma/seed.js || echo 'Seed skipped') && npm start",
    "dev": "cd server && npm run dev",
    "build": "cd client && npm run build"
  }
}
